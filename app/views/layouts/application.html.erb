<!DOCTYPE html>
<html>
<head>
  <% if isEventPage? %>
    <title><%= @event.title %> - <%= @event.host.present? ? @event.host : @event.user.name %></title>
    <meta property="og:title" content="<%= @event.title %> - <%= @event.host.present? ? @event.host : @event.user.name %>">
    <meta property="og:site_name" content="360 Board">
    <meta property="og:description" content="<%= @event.desc_short %>">
    <meta property="og:image" content="<%= events_picture_url(@event.id,"main") %>">
    <meta name="description" content="<%= @event.desc_short %>"/>
    <meta name="keywords" content="<%= @event.opendate_short %>,<%= @event.address_title %>,<%= @event.user.name %>" />
  <% else %>
    <title>360 Board</title>
    <meta property="og:title" content="360 Board">
    <meta property="og:site_name" content="360 Board">
    <meta property="og:description" content="「360 Board」では、シンプルなイベント告知ページを作ることができます。目的・ジャンルを問わず、様々なタイプのイベントの告知にお使い頂けます。">
    <meta property="og:image" content="<%= image_url("360.png") %>">
    <meta name="description" content="「360 Board」では、シンプルなイベント告知ページを作ることができます。目的・ジャンルを問わず、様々なタイプのイベントの告知にお使い頂けます。"/>
    <meta name="keywords" content="360,bulletin board,event,イベント,掲示板,告知,情報,<%= params[:q] %>" />
  <% end %>
  <meta property="og:url" content="<%= request.url %>" />
  <%= javascript_include_tag 'application' %>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
  <!-- Latest compiled and minified JavaScript -->
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="fb-root"></div>
  <script>
  ( function(d, s, id) {
    var js,
    fjs = d.getElementsByTagName(s)[0];
    if (d.getElementById(id))
      return;
      js = d.createElement(s);
      js.id = id;
      js.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1&appId=1471100926452630&version=v2.0";
      fjs.parentNode.insertBefore(js, fjs);
    }(document, 'script', 'facebook-jssdk'));
  </script>
  <%= render 'layouts/navbar' %>
  <div class="container">
    <% if notice %>
    <p class="alert alert-info"><%= notice %></p>
    <% end %>
    <% if alert %>
    <p class="alert alert-warning"><%= alert %></p>
    <% end %>
    <div class="row">
      <div class="col-md-12">
        <div class="alert alert-danger" role="alert">
          勉強会の開催情報は新しいサイト(
          <%= link_to 'dojo.lmlab.net', "https://dojo.lmlab.net", class: 'alert-link' %>
          )に移行しました。今後の開催予定の確認・お申込は新サイトからお願い致します。
          2018.05.03
        </div>
      </div>
    </div>
    <%= yield %>
    <%= render 'layouts/google_ad' %>
  </div>
  <footer class="footer">
    <div class="container">
      <a href="https://github.com/lumbermill/events.lmlab.net">GitHub</a>
      &nbsp;
      <a href="https://lmlab.net">Lmlab.net.</a>
    </div<
  </footer>
</body>
</html>
