<h1>Listing reports</h1>

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Short desc</th>
      <th>Long desc</th>
      <th>Picture 1</th>
      <th>Picture 2</th>
      <th>Picture 3</th>
      <th>Visible</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reports.each do |report| %>
      <tr>
        <td><%= report.event_id %></td>
        <td><%= report.short_desc %></td>
        <td><%= report.long_desc %></td>
        <td><%= report.picture_1 %></td>
        <td><%= report.picture_2 %></td>
        <td><%= report.picture_3 %></td>
        <td><%= report.visible %></td>
        <td><%= link_to 'Show', report %></td>
        <td><%= link_to 'Edit', edit_report_path(report) %></td>
        <td><%= link_to 'Destroy', report, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Report', new_report_path %>
